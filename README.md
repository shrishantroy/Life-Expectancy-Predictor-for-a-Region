# Life-Expectancy-Predictor-for-a-Region
The Machine Learning model uses XGBoost regression algorithm to predict the life expectancy of a definite region. 21 features were used in the dataset. 70% of the dataset was used to train the model and 30% to test the model for its performance. The model has an R2 score of 0.96(approx.), which indicates it works just fine
